const inputText = document.getElementById('input-text');const sortAscBtn = document.getElementById('sort-asc-btn');const sortDescBtn = document.getElementById('sort-desc-btn');const outputText = document.getElementById('output-text');sortAscBtn.addEventListener('click', () => sortText('asc'));sortDescBtn.addEventListener('click', () => sortText('desc'));function sortText(order) {    const lines = inputText.value.split(/\r\n|\n/);    const nonEmptyLines = lines.filter(line => line.trim() !== '');    if (order === 'asc') {        nonEmptyLines.sort();    } else if (order === 'desc') {        nonEmptyLines.sort().reverse();    }    outputText.value = nonEmptyLines.join('\n');}