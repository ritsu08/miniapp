const filenamesInput = document.getElementById('filenames');const searchStringInput = document.getElementById('search-string');const replaceStringInput = document.getElementById('replace-string');const previewBtn = document.getElementById('preview-btn');const previewOutput = document.getElementById('preview-output');previewBtn.addEventListener('click', generatePreview);function generatePreview() {    const filenames = filenamesInput.value.split(/\r\n|\n/).map(name => name.trim()).filter(name => name !== '');    const searchString = searchStringInput.value;    const replaceString = replaceStringInput.value;    if (!searchString) {        previewOutput.value = '検索文字列を入力してください。';        return;    }    const previewLines = filenames.map(filename => {        // Use a global regex to replace all occurrences        const regex = new RegExp(searchString, 'g');        const newFilename = filename.replace(regex, replaceString);        return `${filename} -> ${newFilename}`;    });    previewOutput.value = previewLines.join('\n');}