<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ブログシステム</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <h1>ブログ</h1>
        <nav>
            <% if (user) { %>
                <span>ようこそ、<%= user.username %>さん！</span>
                <a href="/new-post">新しい投稿</a>
                <a href="/logout">ログアウト</a>
            <% } else { %>
                <a href="/login">ログイン</a>
                <a href="/register">新規登録</a>
            <% } %>
        </nav>
        <div class="posts">
            <% if (posts.length > 0) { %>
                <% posts.forEach(post => { %>
                    <div class="post-card">
                        <h2><%= post.title %></h2>
                        <p class="post-meta">投稿者: <%= post.author %> | 日付: <%= post.date %></p>
                        <p><%= post.content %></p>
                    </div>
                <% }); %>
            <% } else { %>
                <p>まだ投稿がありません。</p>
            <% } %>
        </div>
    </div>
</body>
</html>