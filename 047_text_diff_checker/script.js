const text1Input = document.getElementById('text1');const text2Input = document.getElementById('text2');const compareBtn = document.getElementById('compare-btn');const diffOutput = document.getElementById('diff-output');compareBtn.addEventListener('click', compareTexts);function compareTexts() {    const text1 = text1Input.value.split(/\r\n|\n/);    const text2 = text2Input.value.split(/\r\n|\n/);    diffOutput.innerHTML = '';    const maxLength = Math.max(text1.length, text2.length);    for (let i = 0; i < maxLength; i++) {        const line1 = text1[i] !== undefined ? text1[i] : '';        const line2 = text2[i] !== undefined ? text2[i] : '';        const lineDiv = document.createElement('div');        if (line1 === line2) {            lineDiv.textContent = line1;        } else {            if (line1 !== '') {                const span1 = document.createElement('span');                span1.classList.add('removed');                span1.textContent = `- ${line1}`;                lineDiv.appendChild(span1);            }            if (line2 !== '') {                const span2 = document.createElement('span');                span2.classList.add('added');                span2.textContent = `+ ${line2}`;                lineDiv.appendChild(span2);            }        }        diffOutput.appendChild(lineDiv);    }}